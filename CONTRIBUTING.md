# Contributing (for humans + coding agents)

## Non-negotiable rules
- Never commit to `main`. Use milestone branches only.
- Follow `task.md` milestone order.
- Update `PROGRESS.md` at the end of every milestone.
- Do not run installs or create environments automatically. Ask the user.

## Safety
- All sensitive tools require approvals.
- No tool action claims without receipts.